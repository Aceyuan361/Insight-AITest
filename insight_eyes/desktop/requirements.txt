# Insight-Eye Desktop Application Requirements
# 移动设备性能监控桌面应用依赖

# PyQt6 - Qt6的Python绑定
PyQt6>=6.4.0

# pyqtgraph - 高性能科学绘图库
pyqtgraph>=0.13.0

# numpy - 数值计算库
numpy>=1.23.0

# 日志库
logzero>=1.7.0

# 数据库 (可选，用于持久化存储)
# sqlalchemy>=2.0.0
# sqlite3 (Python标准库)

# 数据导出 (可选)
# openpyxl>=3.1.0  # Excel导出
# pandas>=2.0.0     # 数据处理
reportlab>=4.0.0   # PDF导出

# ============================================================
# iOS 设备支持
# ============================================================
# py-ios-device - iOS 性能采集（主要方案，iOS 15-26）
py-ios-device>=0.7.0

# pymobiledevice3 - iOS 设备通信和隧道（iOS 17-26）
pymobiledevice3>=1.0.0

# tidevice - 已弃用，仅用于向后兼容
# 将在 v3.0 版本完全移除
# 推荐使用 py-ios-device 架构代替
# tidevice>=0.9.7

# Android设备支持通过ADB实现，无需额外Python依赖
# 需要系统安装 Android SDK Platform-Tools
