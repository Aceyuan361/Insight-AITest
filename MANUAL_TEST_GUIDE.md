# 手动 GUI 测试指南

## 前提条件
1. 确保在工作树目录中
2. 确保有显示器连接（或使用远程桌面）
3. 确保数据库中有测试会话数据

## 快速启动测试

### 1. 运行应用
```bash
cd .worktrees/report-ui-optimization
python -m insight_eyes.desktop.main
```

### 2. 验证清单

#### 启动验证
- [ ] 应用正常启动，无崩溃
- [ ] 主窗口正常显示
- [ ] 无控制台错误信息

#### 报告页面验证
- [ ] 可以切换到测试报告页面
- [ ] 会话列表正常显示

#### 分屏布局验证
- [ ] 左右分屏布局正常显示
- [ ] 左侧是图表区域（可滚动）
- [ ] 右侧是统计面板
- [ ] 分隔线可以拖动调整
- [ ] 调整后比例合理

#### 会话信息验证
- [ ] 会话信息显示在顶部
- [ ] 信息格式为单行紧凑格式
- [ ] 显示：设备 | 应用 | 时间 | 时长
- [ ] 文字清晰可读

#### 统计面板验证
- [ ] 标题显示"性能统计"
- [ ] FPS 统计卡片正常显示
- [ ] CPU 统计卡片正常显示
- [ ] 内存统计卡片正常显示
- [ ] 网络统计卡片正常显示
- [ ] 告警记录正常显示（如果有）
- [ ] 统计面板可以滚动

#### 操作按钮验证
- [ ] "导出 HTML 报告"按钮显示正确
- [ ] 点击导出按钮显示"开发中"提示
- [ ] "删除会话"按钮显示正确
- [ ] 点击删除按钮显示确认对话框

#### 持久化验证
- [ ] 拖动分隔线调整比例
- [ ] 关闭会话详情
- [ ] 重新打开同一会话
- [ ] 分隔线位置保持不变

## 预期行为

### 正常情况
1. 应用启动流畅，无延迟
2. 界面响应迅速，无卡顿
3. 布局自适应窗口大小
4. 分隔线拖动流畅

### 异常情况处理
1. 如果没有会话数据，应显示提示信息
2. 如果数据库错误，应显示错误对话框
3. 如果会话不存在，应显示警告

## 常见问题

### Q: 应用无法启动
A: 检查依赖是否安装完整：
```bash
pip install -r requirements.txt
```

### Q: 没有会话数据
A: 先运行一次监控来创建测试会话

### Q: 界面显示异常
A: 检查 PyQt6 版本是否正确

### Q: 分隔线无法拖动
A: 检查是否有足够的窗口空间

## 测试完成后

### 如果所有测试通过
- 创建测试通过标记
- 提交测试报告

### 如果发现问题
- 记录问题和复现步骤
- 截图保存
- 报告给开发团队

## 回退测试

如果需要测试之前的版本：
```bash
# 切换到主分支
git worktree remove .worktrees/report-ui-optimization
git checkout main
python -m insight_eyes.desktop.main
```

## 联系方式

如有问题，请联系开发团队或在项目中创建 issue。
